# 2026-07-14



**영화 API 호출하는 법**

talend api에서 Import Postman Collection v2 눌러서 파일을 import 한다. 주소와 Headers, Environments를 작성한다. 그리고 send를 눌러 200이 뜨면 성공이다.

그리고 pub.dev에서 https를 복사해 플러터 pubspec.yaml에 등록한다. 

widget_test.dart에서 호출 코드를 작성하면 됨.

**예시**

```
void main() async {
  final client = Client();

  final base_url = 'https://api.themoviedb.org/3';
  final language = 'ko-KR';
  final region = 'KR';
  final api_key = 'eyJhbGciOiJIUzI1NiJ9.eyJhdWQiOiJiMTBlN2IxMzU3Y2Q4ZWU1ZTZiNzAxMGI4M2E3YTcxZCIsIm5iZiI6MTc4NDAxNjc1OS45NDgsInN1YiI6IjZhNTVlZjc3MmJiZDY0OTY0ZWVmMjZmZSIsInNjb3BlcyI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.UV2hCogHi1RUCGllbyOlHe3fMZ-QLdgG-tGqZgGFsDE';
  final url = Uri.parse('${base_url}/genre/movie/list?language=${language}&region=${region}');
  final response = await client.get(url, headers: {'Authorization': 'Bearer ${api_key}'});

  final jsonBody = jsonDecode(response.body);
  final genreJsonList = jsonBody['genres'] as List <dynamic>;
  final genreModelList = genreJsonList.map((e) {
    return GenreModel.fromJson(e);
  },);

  for(var value in genreModelList) {
    print(value.name);
  }
}
```



**출력값**

```
액션
모험
애니메이션
코미디
범죄
다큐멘터리
드라마
가족
판타지
역사
공포
음악
미스터리
로맨스
SF
TV 영화
스릴러
전쟁
서부

```


