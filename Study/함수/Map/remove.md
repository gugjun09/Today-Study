# remove

Map에서 key에 해당하는 key-value 요소를 제거하는 함수.

```
Map<String, int> myMap = {
      'a': 1,
      'b': 2,
      'c': 3,
    };
    print(myMap);  // a: 1, b: 2, c: 3

    myMap.remove('b');
    print(myMap);   // a: 1, c: 3
```

