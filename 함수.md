**함수 Function (펑션)**

뜻: 입력값을 넣으면 정해진 규칙에 따라 결과값을 출력하는 개념

**메소드 Method (뜻: 방식)**

뜻: 클래스 내부나 객체 내부에 선언된 함수.

# String

substring()

문자열에서 start와 end를 정해서 자를 수 있게 만들어주는 함수

```
const string = 'dartlang';
var result = string.substring(1); // 'artlang'
result = string.substring(1, 4); // 'art'
```

length

문자열의 길이 출력용 함수. 반환 값이 Int라서 Text 같은 곳에서 사용할 땐 String 으로 변환 해줘야 함.

```
'Dart'.length;          // 4
'Dart'.runes.length;    // 4
var clef = '\\u{1D11E}';
clef.length;            // 2
clef.runes.length;      // 1
```

**List**

**DateTime**

데이트 타임 함수.

now()

현재 시간 출력 함수. 반환 값은 DateTime.



